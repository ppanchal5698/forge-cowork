# /skills

Short-lived, **stateless** functions. Deploy target: AWS Lambda (MiniStack Lambda locally).
Long-running or stateful work does not belong here — it goes in `/plugins` (ECS tasks).
See CLAUDE.md §6. First skills land in Sprint 2.
